# LiSync (LiSynchronization)

LiSync 是一款由 Mindrfit 维护的 Android 原生应用，用于**兼容洛雪音源的手机同步器**。  
它在手机端作为中转服务，负责与手表端（Vela 快应用）协作完成音乐链接解析、搜索与歌词请求的转发与响应。

核心能力：
- 音源脚本解析与播放链接中转
- 内置搜索与歌词获取（tx/wy/kg）
- 主题传输：支持选择 `theme.zip` 自动解压后按协议发送

文档入口：`docs/`（协议在 `docs/protocols/`，主题规范在 `docs/theme/`）。
